function sum(){
    let firstNumber = prompt("Введите число:");
    let secondNumber = prompt("Введите число:");
    let total = +firstNumber + +secondNumber;
        alert(`Сумма равна ${total}`);
    }

function difference(){
    let firstNumber = prompt("Введите число:");
    let secondNumber = prompt("Введите число:");
    let total = +firstNumber - +secondNumber;
    alert(`Разность равна ${total}`);
    }

function work(){
    let firstNumber = prompt("Введите число:");
    let secondNumber = prompt("Введите число:");
    let total = +firstNumber * +secondNumber;
    alert(`Произведение равно ${total}`);
    }

function private(){
    let firstNumber = prompt("Введите число:");
    let secondNumber = prompt("Введите число:");
    let total = +firstNumber / +secondNumber;
    alert(`Частное равно ${total}`);
    }